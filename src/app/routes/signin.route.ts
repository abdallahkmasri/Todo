import { Routes } from "@angular/router";
import { SignInComponent } from "../components/signIn/form.component";

export const SignInRoutes: Routes = [
    {
        path: '',
        component: SignInComponent
    }
]